apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-tetris-game.fullname" . }}-test-connection"
  labels:
    {{- include "my-tetris-game.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-tetris-game.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
