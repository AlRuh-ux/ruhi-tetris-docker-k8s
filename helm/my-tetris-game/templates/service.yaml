apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-tetris-game.fullname" . }}
  labels:
    {{- include "my-tetris-game.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-tetris-game.selectorLabels" . | nindent 4 }}
